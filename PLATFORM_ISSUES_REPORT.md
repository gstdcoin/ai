# 🔴 Отчет о проблемах платформы GSTD
**Дата:** 2026-01-18T20:25:00Z  
**Статус:** ⚠️ Обнаружены критические и некритические проблемы

---

## 📊 Executive Summary

Платформа функционирует, но обнаружены следующие проблемы:
- ✅ **Инфраструктура:** Все контейнеры работают (7/7 healthy)
- ⚠️ **Frontend:** React Error #300 (критическая ошибка хуков)
- ⚠️ **Backend:** Проблемы с миграцией БД и TON API
- ⚠️ **Авторизация:** 401 ошибки при неавторизованных запросах (ожидаемое поведение)

---

## 🔴 Критические проблемы

### 1. React Error #300 - Нарушение правил хуков
**Приоритет:** 🔴 КРИТИЧЕСКИЙ  
**Файл:** `frontend/src/components/common/WalletListener.tsx`  
**Описание:** Ошибка "Rendered more hooks than during the previous render"

**Причина:**
- Неиспользуемая функция `useBalanceRefresh()` определена в файле (строка 213), но не вызывается
- Возможны условные вызовы хуков в компоненте
- React требует, чтобы хуки вызывались в одинаковом порядке при каждом рендере

**Влияние:**
- Приложение падает с ошибкой в ErrorBoundary
- Пользователи видят экран ошибки вместо интерфейса
- Невозможно использовать функциональность приложения

**Решение:**
```typescript
// Удалить неиспользуемую функцию useBalanceRefresh() (строки 212-239)
// Или правильно использовать её, если она нужна
```

**Статус:** ❌ Не исправлено

---

### 2. Миграция базы данных v15 не применена
**Приоритет:** 🔴 КРИТИЧЕСКИЙ  
**Файл:** `backend/migrations/v15_payout_tracking.sql`  
**Ошибка:** `foreign key constraint "fk_intent_task" cannot be implemented`

**Причина:**
- Foreign key constraint ссылается на таблицу `tasks` с колонкой `task_id`
- Возможно, структура таблицы `tasks` не соответствует ожидаемой
- Или таблица `tasks` не существует

**Влияние:**
- Система отслеживания выплат не работает
- Невозможно обеспечить идемпотентность выплат
- Риск дублирования транзакций

**Решение:**
1. Проверить структуру таблицы `tasks`:
   ```sql
   \d tasks
   ```
2. Убедиться, что `task_id` существует и имеет правильный тип (UUID)
3. Применить миграцию вручную или исправить constraint

**Статус:** ❌ Не исправлено

---

## ⚠️ Некритические проблемы

### 3. TON API ошибки декодирования адресов
**Приоритет:** ⚠️ СРЕДНИЙ  
**Логи:** Backend показывает ошибки:
```
TON API error (status 400): {"error":"can't decode address EQAKRcWU1Gy5tSntSHuWD9JxSosKJ9_VAIux1BTRruSmGpAA"}
```

**Причина:**
- Адреса TON передаются в неправильном формате
- Возможно, используется raw address вместо user-friendly формата
- TON API не может декодировать адрес

**Влияние:**
- Не удается получить баланс TON для некоторых адресов
- Возвращается 0 баланс (fallback)
- Функциональность работает, но с ограничениями

**Решение:**
- Проверить формат адресов перед отправкой в TON API
- Конвертировать raw address в правильный формат
- Добавить валидацию адресов

**Статус:** ⚠️ Частично работает (fallback на 0)

---

### 4. 401 Unauthorized при запросах без авторизации
**Приоритет:** ℹ️ ИНФОРМАЦИОННЫЙ  
**Эндпоинт:** `/api/v1/stats`

**Описание:**
- Приложение пытается получить `/api/v1/stats` без авторизации
- API возвращает 401 (ожидаемое поведение)
- В консоли появляются предупреждения

**Причина:**
- Компоненты пытаются загрузить данные до авторизации пользователя
- Нет проверки авторизации перед запросами

**Влияние:**
- Много предупреждений в консоли
- Не критично, но засоряет логи

**Решение:**
- Добавить проверку авторизации перед запросами к защищенным эндпоинтам
- Использовать публичные эндпоинты для неавторизованных пользователей
- Добавить обработку 401 ошибок с редиректом на логин

**Статус:** ⚠️ Работает, но с предупреждениями

---

### 5. Предупреждение Nginx о deprecated директиве
**Приоритет:** ℹ️ ИНФОРМАЦИОННЫЙ  
**Файл:** `nginx/conf.d/load-balancer.conf:40`  
**Ошибка:** `the "listen ... http2" directive is deprecated`

**Причина:**
- Используется устаревший синтаксис `listen 443 ssl http2;`
- Новый синтаксис: `listen 443 ssl;` и отдельная директива `http2 on;`

**Влияние:**
- Предупреждение в логах
- HTTP/2 все еще работает

**Решение:**
```nginx
# Старый синтаксис:
listen 443 ssl http2;

# Новый синтаксис:
listen 443 ssl;
http2 on;
```

**Статус:** ⚠️ Работает, но с предупреждением

---

### 6. Проблемы с подключением к PostgreSQL
**Приоритет:** ⚠️ СРЕДНИЙ  
**Ошибка:** `role "gstd_user" does not exist`

**Причина:**
- Скрипты пытаются подключиться с пользователем `gstd_user`
- В контейнере используется пользователь `postgres`

**Влияние:**
- Невозможно выполнить некоторые SQL команды вручную
- Автоматические скрипты могут не работать

**Решение:**
- Использовать правильного пользователя (`postgres`) или создать `gstd_user`
- Обновить скрипты с правильными credentials

**Статус:** ⚠️ Частично работает (контейнер работает, но скрипты могут падать)

---

## 📈 Статус инфраструктуры

### Контейнеры Docker
| Контейнер | Статус | CPU | Memory | Uptime |
|-----------|--------|-----|---------|--------|
| `gstd_nginx_lb` | ✅ Healthy | 0.00% | 8.4 MB | 7 минут |
| `ubuntu-backend-blue-1` | ✅ Healthy | 1.46% | 22.1 MB | 7 минут |
| `ubuntu-backend-green-1` | ✅ Healthy | 1.33% | 20.9 MB | 7 минут |
| `ubuntu-frontend-1` | ✅ Healthy | 0.00% | 45.6 MB | 7 минут |
| `ubuntu-frontend-2` | ✅ Healthy | 0.00% | 45.8 MB | 7 минут |
| `gstd_postgres_prod` | ✅ Healthy | 0.10% | 63.1 MB | 8 минут |
| `gstd_redis_prod` | ✅ Healthy | 0.65% | 3.7 MB | 8 минут |

**Всего:** 7/7 контейнеров работают ✅

### API Health Checks
- ✅ `/api/v1/health` → 200 OK
- ✅ `/api/v1/stats/public` → 200 OK
- ⚠️ `/api/v1/stats` → 401 (требует авторизацию, ожидаемо)

---

## 🔍 Анализ логов

### Nginx Access Log
- Большинство запросов возвращают 200 OK
- Некоторые запросы возвращают 401 (неавторизованные)
- Нет записей о 502 ошибках в последних логах
- Статус 499 (client closed connection) - клиент закрыл соединение до получения ответа

### Backend Logs
- ⚠️ TON API ошибки декодирования адресов
- ⚠️ Ошибка миграции v15
- ✅ Остальные запросы обрабатываются успешно

### Frontend Logs
- 🔴 React Error #300 (критическая ошибка)
- ⚠️ Предупреждения о tonProof
- ⚠️ 401 ошибки при неавторизованных запросах

---

## 🎯 План действий

### Немедленные действия (критично)

1. **Исправить React Error #300**
   - Удалить неиспользуемую функцию `useBalanceRefresh()` из `WalletListener.tsx`
   - Проверить все условные вызовы хуков
   - Протестировать после исправления

2. **Исправить миграцию v15**
   - Проверить структуру таблицы `tasks`
   - Исправить foreign key constraint
   - Применить миграцию

### Краткосрочные действия (1-2 дня)

3. **Исправить TON API ошибки**
   - Добавить валидацию и конвертацию адресов
   - Обработать ошибки декодирования

4. **Улучшить обработку авторизации**
   - Добавить проверки перед запросами
   - Использовать публичные эндпоинты для неавторизованных

5. **Обновить конфигурацию Nginx**
   - Исправить deprecated директиву http2

### Долгосрочные улучшения

6. **Улучшить мониторинг**
   - Добавить алерты на критические ошибки
   - Настроить сбор метрик

7. **Документация**
   - Обновить документацию по troubleshooting
   - Добавить runbook для типичных проблем

---

## 📝 Рекомендации

1. **Приоритизация:** Сначала исправить React Error #300, так как это блокирует использование приложения

2. **Тестирование:** После каждого исправления провести полное тестирование

3. **Мониторинг:** Настроить алерты на критические ошибки (502, 500, React errors)

4. **Логирование:** Улучшить логирование для лучшей диагностики проблем

---

## 🔗 Связанные файлы

- `frontend/src/components/common/WalletListener.tsx` - React Error #300
- `backend/migrations/v15_payout_tracking.sql` - Проблема миграции
- `nginx/conf.d/load-balancer.conf` - Deprecated директива
- `PLATFORM_STATUS.md` - Общий статус платформы

---

**Следующий отчет:** После исправления критических проблем

*Сгенерировано автоматически: 2026-01-18T20:25:00Z*
