# 🔧 Исправления подключения кошелька

## Проблема
После сканирования QR кода ничего не происходило - кошелек не подключался.

## Исправления

### 1. Улучшена обработка событий подключения
- **onStatusChange** теперь правильно обрабатывает все события подключения
- Добавлено автоматическое закрытие модального окна после успешного подключения
- Улучшено логирование для отладки

### 2. Добавлен периодический мониторинг подключения
- После открытия модального окна запускается проверка каждую секунду
- Это гарантирует, что подключение будет обнаружено даже если события не сработали
- Проверка автоматически останавливается после подключения или через 30 секунд

### 3. Улучшена обработка состояния кошелька
- Исправлена логика определения подключенного кошелька
- Предотвращены пропуски событий подключения
- Улучшена обработка изменений состояния

### 4. Добавлена обратная связь для пользователя
- Показывается уведомление при открытии модального окна
- Пользователь видит, что нужно сканировать QR код
- Добавлены переводы для сообщений

## Как проверить

1. **Откройте платформу**: https://app.gstdtoken.com
2. **Нажмите "Подключить кошелек"**
3. **Сканируйте QR код** в кошельке
4. **Ожидаемое поведение**:
   - Появится уведомление "Сканируйте QR код в кошельке"
   - После сканирования модальное окно закроется автоматически
   - Кошелек подключится и выполнится вход
   - Появится сообщение об успешном подключении

## Отладка

Если проблема сохраняется:

### 1. Проверьте консоль браузера
Откройте DevTools (F12) → Console и проверьте логи:
- Должны быть сообщения: "Wallet connected via onStatusChange"
- Должны быть сообщения: "User logged in successfully"

### 2. Проверьте Network tab
- Убедитесь, что запрос к `/api/v1/users/login` выполняется
- Проверьте статус ответа (должен быть 200)

### 3. Проверьте manifest файл
```bash
curl https://app.gstdtoken.com/tonconnect-manifest.json
```
Должен вернуть JSON с информацией о приложении.

### 4. Проверьте логи на сервере
```bash
docker logs ubuntu_frontend_1 --tail 50
```

## Возможные проблемы

### Проблема: QR код не сканируется
**Решение**: 
- Убедитесь, что используете поддерживаемый кошелек (Tonkeeper, TON Wallet и т.д.)
- Проверьте, что manifest файл доступен

### Проблема: После сканирования ничего не происходит
**Решение**:
- Проверьте консоль браузера на ошибки
- Убедитесь, что API доступен: `curl https://app.gstdtoken.com/api/v1/health`
- Проверьте, что бэкенд работает: `docker ps | grep backend`

### Проблема: Модальное окно не закрывается
**Решение**:
- Это исправлено в новой версии - модальное окно закрывается автоматически
- Если проблема сохраняется, обновите страницу

## Технические детали

### Изменения в коде:

1. **WalletConnect.tsx**:
   - Улучшен `onStatusChange` handler
   - Добавлен периодический мониторинг подключения
   - Добавлено автоматическое закрытие модального окна
   - Улучшено логирование

2. **Локализация**:
   - Добавлены переводы для сообщений подключения
   - EN: "Scan QR code in wallet", "Waiting for connection..."
   - RU: "Сканируйте QR код в кошельке", "Ожидание подключения..."

## Следующие шаги

Если проблема не решена:
1. Проверьте логи в консоли браузера
2. Проверьте логи на сервере
3. Убедитесь, что все сервисы работают
4. Проверьте доступность API

---

**Дата исправления**: 11 января 2026  
**Версия**: 1.1
