# 🔧 Frontend Fix Summary - API URL Configuration

**Дата:** 11 января 2026  
**Проблема:** Ошибка 'Failed to load tasks' и желтая кнопка TonConnect

---

## ✅ Исправления

### 1. Исправлен NEXT_PUBLIC_API_URL в docker-compose.yml

**Было:**
```yaml
environment:
  - NEXT_PUBLIC_API_URL=https://app.gstdtoken.com/api
```

**Стало:**
```yaml
environment:
  - NEXT_PUBLIC_API_URL=https://app.gstdtoken.com
```

**Причина:**
- Frontend код уже добавляет `/api/v1/...` к базовому URL
- Двойной `/api` приводил к неправильному URL: `https://app.gstdtoken.com/api/api/v1/tasks`
- Теперь правильный URL: `https://app.gstdtoken.com/api/v1/tasks` ✅

---

### 2. Проверка TonConnect Manifest

**Статус:** ✅ **Работает корректно**

- Manifest URL: `https://app.gstdtoken.com/tonconnect-manifest.json`
- Доступен по HTTPS: ✅
- Содержит правильные данные: ✅

**Проверка:**
```bash
curl https://app.gstdtoken.com/tonconnect-manifest.json
# Результат: JSON с корректными данными ✅
```

---

### 3. Желтая кнопка TonConnect

**Статус:** ✅ **Это нормально**

Желтый цвет кнопки установлен намеренно в `_app.tsx`:
```typescript
colorsSet: {
  [THEME.DARK]: {
    connectButton: {
      background: '#FFD700',  // Золотой/желтый цвет
      foreground: '#0a1929'
    }
  }
}
```

Это не ошибка, а дизайн-решение.

---

## 🔄 Что нужно сделать

### Важно: Пересобрать frontend образ

Next.js кэширует переменные окружения при сборке. Чтобы изменения вступили в силу:

```bash
# 1. Пересобрать frontend образ с новыми переменными
docker compose build frontend

# 2. Перезапустить контейнер
docker compose up -d frontend
```

Или если используете готовый образ:

```bash
# Просто перезапустить (если переменные передаются через environment)
docker compose restart frontend
```

---

## 📋 Проверка работоспособности

После перезапуска проверьте:

1. **API запросы работают:**
   ```bash
   curl https://app.gstdtoken.com/api/v1/tasks
   # Должен вернуть JSON с задачами
   ```

2. **Frontend загружает задачи:**
   - Откройте https://app.gstdtoken.com
   - Проверьте консоль браузера (F12)
   - Не должно быть ошибок "Failed to load tasks"

3. **TonConnect работает:**
   - Кнопка должна быть желтой (это нормально)
   - При клике должно открываться окно подключения кошелька

---

## 🎯 Итог

- ✅ Исправлен NEXT_PUBLIC_API_URL (убран двойной `/api`)
- ✅ Manifest URL правильный и доступен
- ✅ Желтая кнопка - это нормально (дизайн)

**Следующий шаг:** Пересобрать/перезапустить frontend контейнер для применения изменений.

---

**Дата:** 11 января 2026  
**Статус:** ✅ Fix Applied
