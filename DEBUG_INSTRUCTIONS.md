# Инструкция по отладке проблемы с QR-кодом

## Шаг 1: Откройте консоль браузера

1. Откройте https://app.gstdtoken.com
2. Нажмите **F12** (или **Cmd+Option+I** на Mac)
3. Перейдите на вкладку **Console**

## Шаг 2: Проверьте инициализацию

После загрузки страницы должны появиться логи:
- `🔧 TonConnectUI initialized` ✅
- `📱 Connected: false` (если не подключено)
- `👤 Account: null` (если не подключено)

**Если этих логов нет** - проблема с загрузкой TonConnect.

## Шаг 3: Попробуйте подключиться

1. Нажмите кнопку **"Подключить кошелек"**
2. Должны появиться логи:
   - `🔌 Opening TonConnect modal...`
   - `✅ Modal opened`

**Если модальное окно не открывается** - проверьте ошибки в консоли.

## Шаг 4: Отсканируйте QR-код

1. Откройте кошелек (Tonkeeper, MyTonWallet и т.д.)
2. Отсканируйте QR-код
3. Должен появиться лог:
   - `✅ TonConnect account connected`

**Если после сканирования ничего не происходит:**

### Проверьте в консоли:
- Есть ли ошибки (красные сообщения)?
- Есть ли предупреждения (желтые сообщения)?

### Проверьте Network tab:
1. В DevTools перейдите на вкладку **Network**
2. Отфильтруйте по `tonconnect` или `manifest`
3. Проверьте запросы:
   - `tonconnect-manifest.json` - должен быть статус 200
   - Другие запросы к tonconnect - проверьте статусы

## Шаг 5: Проверьте manifest.json

В консоли браузера выполните:
```javascript
fetch('https://app.gstdtoken.com/tonconnect-manifest.json')
  .then(r => r.json())
  .then(console.log)
```

Должен вернуться JSON объект с полями:
- `url`
- `name`
- `iconUrl`
- `termsOfUseUrl`
- `privacyPolicyUrl`

## Шаг 6: Проверьте объект TonConnectUI

В консоли браузера выполните:
```javascript
// Подождите несколько секунд после загрузки страницы
window.tonConnectUI
```

Должен вернуться объект с методами:
- `openModal()`
- `disconnect()`
- `account`
- `connected`

## Возможные проблемы и решения

### Проблема: "TonConnectUI не инициализирован"
**Решение:**
- Обновите страницу (Ctrl+F5)
- Проверьте, что manifest.json доступен
- Проверьте консоль на ошибки загрузки

### Проблема: QR-код появляется, но ничего не происходит
**Решение:**
- Убедитесь, что используете поддерживаемый кошелек
- Проверьте, что кошелек обновлен до последней версии
- Попробуйте другой кошелек (Tonkeeper, MyTonWallet)
- Проверьте, что используете правильную сеть (mainnet)

### Проблема: Ошибка CORS
**Решение:**
- Проверьте, что manifest.json доступен по HTTPS
- Проверьте настройки Nginx

### Проблема: Ошибка загрузки manifest
**Решение:**
```bash
curl https://app.gstdtoken.com/tonconnect-manifest.json
```
Должен вернуть JSON без ошибок.

## Что нужно прислать для диагностики

Если проблема сохраняется, пришлите:

1. **Все сообщения из консоли** (скриншот или текст)
2. **Ошибки из Network tab** (если есть)
3. **Какой кошелек используете** (Tonkeeper, MyTonWallet и т.д.)
4. **Версию кошелька**
5. **Браузер и его версию**

Это поможет точно определить проблему.

