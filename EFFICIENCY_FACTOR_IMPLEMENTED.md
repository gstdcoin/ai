# ✅ Коэффициент эффективности GSTD реализован

## 🎯 Формула

**EfficiencyFactor(G) = α + (1 − α) / (1 + ln(1 + G / K))**

Где:
- **G** = баланс GSTD
- **α** = 0.30 (нижний предел)
- **K** = 10,000 GSTD (нормализующий коэффициент)
- **ln** = натуральный логарифм

## 📊 Таблица эффекта

| GSTD | EfficiencyFactor | Экономия TON |
|------|------------------|--------------|
| 0    | 1.00             | 0%           |
| 100  | 0.78             | 22%          |
| 1,000| 0.60             | 40%          |
| 10,000| 0.43            | 57%          |
| 100,000| 0.34           | 66%          |
| 1,000,000| 0.30        | 70% (потолок)|

## ✅ Что реализовано

### 1. EfficiencyService
- ✅ Функция `CalculateEfficiency(gstdBalance)` - расчет коэффициента
- ✅ Функция `CalculateTaskCost(baseCost, gstdBalance)` - расчет финальной стоимости
- ✅ Функция `CalculatePriority(taskValue, gstdBalance)` - расчет приоритета
- ✅ Функция `GetEfficiencyBreakdown(gstdBalance)` - детальная информация

### 2. Интеграция в TaskService
- ✅ При создании задания автоматически рассчитывается efficiency
- ✅ Финальная стоимость = базовая стоимость × efficiency
- ✅ Приоритет = taskValue / efficiency (выше efficiency = выше приоритет)
- ✅ Escrow использует финальную стоимость (после скидки)

### 3. API Endpoint
- ✅ `GET /api/v1/wallet/efficiency?address=<address>`
- ✅ Возвращает:
  - `gstd_balance` - баланс GSTD
  - `efficiency` - коэффициент эффективности (0.30 - 1.0)
  - `cost_reduction_percent` - процент экономии (0-70%)
  - `final_cost_multiplier` - множитель для базовой стоимости
  - `priority_multiplier` - множитель для приоритета

## 🔧 Как работает

### Создание задания

1. **Получение баланса GSTD** заказчика
2. **Расчет efficiency** по формуле
3. **Расчет финальной стоимости**: `finalCost = baseCost × efficiency`
4. **Расчет приоритета**: `priority = taskValue / efficiency`
5. **Сохранение** с финальной стоимостью в escrow

### Пример

```go
// Заказчик с 1,000 GSTD
gstdBalance := 1000.0
baseCost := 0.05 TON

// Efficiency = 0.60 (40% экономия)
efficiency := 0.60
finalCost := 0.05 × 0.60 = 0.03 TON

// Приоритет выше (меньше efficiency = выше приоритет)
priority := 0.05 / 0.60 = 0.083
```

## 📝 API Использование

### Получить efficiency для адреса

```bash
curl "https://app.gstdtoken.com/api/v1/wallet/efficiency?address=EQD..."
```

**Ответ:**
```json
{
  "gstd_balance": 1000.0,
  "efficiency": 0.60,
  "cost_reduction_percent": 40.0,
  "final_cost_multiplier": 0.60,
  "priority_multiplier": 1.67
}
```

### Создание задания

При создании задания через `POST /api/v1/tasks`:
- Автоматически рассчитывается efficiency
- Финальная стоимость применяется к `reward_amount_ton`
- Приоритет рассчитывается автоматически

## 🎯 Экономическая логика

### Преимущества модели

1. **Нет порогов входа** - даже 0 GSTD = 100% стоимость (работает)
2. **Масштабируемость** - мелкие участники выигрывают
3. **Нет монополии** - даже "киты" не получают 100% скидку
4. **Экономический смысл** - держать GSTD выгодно
5. **Рост цены GSTD** - усиливает эффект

### Эффект на платформу

- **Больше задач** → больше TON оборота
- **Больше оборота** → больше buyback GSTD
- **Больше buyback** → рост цены GSTD
- **Рост цены** → больше желание держать GSTD
- **Больше GSTD** → больше efficiency → больше задач

## 📚 Документация

### Код

- `backend/internal/services/efficiency_service.go` - основной сервис
- `backend/internal/services/task_service.go` - интеграция
- `backend/internal/api/routes.go` - API endpoint

### Формулы

1. **Efficiency**: `α + (1 - α) / (1 + ln(1 + G / K))`
2. **Final Cost**: `baseCost × efficiency`
3. **Priority**: `taskValue / efficiency`

## ✅ Готово к использованию!

Система автоматически:
- ✅ Рассчитывает efficiency при создании задания
- ✅ Применяет скидку к стоимости
- ✅ Устанавливает приоритет в очереди
- ✅ Использует финальную стоимость для escrow

**No Limits. No Barriers. Pure Efficiency.**

