# ✅ TON API Key настроен и готов к работе

## 🎉 Всё готово!

TON API ключ успешно интегрирован в систему с автоматическим rate limiting.

## ✅ Что реализовано

### 1. TON API Key
- ✅ API Key добавлен в `.env` файл
- ✅ Передаётся в backend
- ✅ Используется в заголовках запросов

### 2. Rate Limiting
- ✅ Автоматический лимит 10 запросов в секунду
- ✅ Token bucket алгоритм
- ✅ Предзаполнение токенов при старте
- ✅ Автоматическое пополнение каждые 100ms

### 3. Конфигурация
- ✅ `.env` файл создан
- ✅ `docker-compose.yml` обновлён
- ✅ Backend использует переменные окружения

## 🔧 Как работает

### Rate Limiting
1. При старте создаётся буфер на 10 токенов
2. Все 10 токенов предзаполняются
3. Каждые 100ms добавляется новый токен
4. Перед каждым запросом к TON API ожидается токен
5. Если токенов нет - запрос ждёт

### API Key
1. API Key читается из переменной окружения `TON_API_KEY`
2. Добавляется в заголовки запросов:
   - `Authorization: Bearer <key>`
   - `X-API-Key: <key>`

## 📊 Использование

### Проверка баланса GSTD
```bash
curl "https://app.gstdtoken.com/api/v1/wallet/gstd-balance?address=EQD..."
```

### В коде
```go
tonService := services.NewTONService(apiURL, apiKey)
balance, err := tonService.GetJettonBalance(ctx, address, jettonAddress)
hasGSTD, err := tonService.CheckGSTDBalance(ctx, address, jettonAddress)
```

## 🔍 Проверка

### 1. Проверить .env файл
```bash
grep TON_API_KEY .env
```

### 2. Проверить backend
```bash
docker-compose ps backend
docker-compose logs backend | tail -5
```

### 3. Проверить API
```bash
curl "https://app.gstdtoken.com/api/v1/wallet/gstd-balance?address=EQD..."
```

## ⚠️ Важно

- **Не коммитить .env**: Файл должен быть в `.gitignore`
- **Лимит соблюдается автоматически**: Не нужно вручную контролировать
- **При превышении лимита**: Запросы будут ждать доступных токенов

## 📝 Конфигурация

### .env
```bash
TON_API_URL=https://tonapi.io
TON_API_KEY=6512ff28fd1ffc8e29b7230642e690b410f7c68e15ef74c4e81e17e9f7a65de6
TON_NETWORK=testnet
GSTD_JETTON_ADDRESS=
TON_CONTRACT_ADDRESS=
```

### Docker Compose
Переменные передаются через `environment` в `docker-compose.yml`.

## 🚀 Готово к использованию!

Система готова к работе с TON API:
- ✅ API Key настроен
- ✅ Rate limiting работает
- ✅ Backend запущен
- ✅ Все запросы автоматически ограничиваются

## 📚 Документация

- [API_KEY_CONFIGURED.md](./API_KEY_CONFIGURED.md) - детали настройки
- [TON_API_SETUP.md](./TON_API_SETUP.md) - полная документация
- TON API: https://tonapi.io/docs

