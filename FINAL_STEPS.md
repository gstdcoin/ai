# Финальные шаги для запуска

## ✅ Что уже исправлено

1. **Backend Dockerfile** - исправлен порядок сборки, go.sum генерируется автоматически
2. **go.mod** - удалён проблемный пакет tonkeeper/tongo
3. **go.sum** - создан с правильными зависимостями (122 строки)
4. **Скрипты** - добавлены скрипты для автоматизации

## 🚀 Быстрый запуск

### Вариант 1: Полностью автоматический

```bash
# Запустить автоматическую настройку
./scripts/setup-production.sh
```

### Вариант 2: Пошаговый

```bash
# 1. Создать директории
mkdir -p nginx/ssl nginx/certbot
chmod 755 nginx/certbot

# 2. Проверить что go.sum существует (уже создан)
ls -lh backend/go.sum

# 3. Получить SSL сертификат
./scripts/init-letsencrypt.sh

# 4. Запустить все сервисы
docker-compose up -d --build

# 5. Проверить статус
docker-compose ps
```

## 🔍 Проверка работы

```bash
# Статус сервисов
docker-compose ps

# Логи
docker-compose logs -f

# Тест сайта
curl -I https://app.gstdtoken.com

# Проверка SSL
docker-compose exec certbot certbot certificates
```

## ⚠️ Если backend не собирается

```bash
# Пересобрать только backend
docker-compose build --no-cache backend

# Или использовать скрипт
./scripts/build-backend.sh
```

## ⚠️ Если SSL не получается

1. Убедитесь что DNS настроен: `dig app.gstdtoken.com`
2. Убедитесь что порт 80 открыт
3. Проверьте что Nginx запущен: `docker-compose ps nginx`
4. Проверьте логи: `docker-compose logs nginx certbot`

## 📝 Следующие шаги

После успешного запуска:

1. Инициализируйте базу данных (SQL из DATABASE_SCHEMA.md)
2. Настройте переменные окружения (.env)
3. Проверьте работу API: `curl https://app.gstdtoken.com/api/v1/stats`
4. Откройте в браузере: https://app.gstdtoken.com

## 📚 Документация

- [QUICK_START.md](./QUICK_START.md) - Быстрый старт
- [DEPLOYMENT.md](./DEPLOYMENT.md) - Полная инструкция
- [TROUBLESHOOTING.md](./TROUBLESHOOTING.md) - Решение проблем
- [FIXES.md](./FIXES.md) - Исправления



