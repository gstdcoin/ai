# ✅ TON API Key настроен

## 🔑 API Key добавлен

- **API Key**: `6512ff28fd1ffc8e29b7230642e690b410f7c68e15ef74c4e81e17e9f7a65de6`
- **Лимит**: 10 запросов в секунду
- **Rate Limiting**: ✅ Реализован автоматически

## 📝 Что сделано

### 1. Обновлён TONService
- ✅ Добавлена поддержка API ключа
- ✅ Реализован rate limiting (10 req/s)
- ✅ API ключ добавляется в заголовки запросов

### 2. Обновлена конфигурация
- ✅ Добавлены переменные окружения в `docker-compose.yml`
- ✅ Создан `.env` файл с API ключом
- ✅ Backend использует API ключ из конфигурации

### 3. Rate Limiting
- ✅ Token bucket алгоритм
- ✅ Автоматическое соблюдение лимита 10 req/s
- ✅ Предзаполнение токенов при старте

## 🔧 Конфигурация

### .env файл
```bash
TON_API_URL=https://tonapi.io
TON_API_KEY=6512ff28fd1ffc8e29b7230642e690b410f7c68e15ef74c4e81e17e9f7a65de6
TON_NETWORK=testnet
GSTD_JETTON_ADDRESS=
TON_CONTRACT_ADDRESS=
```

### Docker Compose
Переменные передаются в контейнер backend через `environment`.

## 🚀 Как работает Rate Limiting

1. **Token Bucket**: Буфер на 10 токенов
2. **Предзаполнение**: При старте все 10 токенов доступны
3. **Пополнение**: Каждые 100ms добавляется новый токен
4. **Ожидание**: Если токенов нет, запрос ждёт

## 📊 Использование

### Проверка баланса GSTD
```bash
curl "https://app.gstdtoken.com/api/v1/wallet/gstd-balance?address=EQD..."
```

### В коде
```go
tonService := services.NewTONService(apiURL, apiKey)
balance, err := tonService.GetJettonBalance(ctx, address, jettonAddress)
```

## ✅ Проверка

1. **Backend запущен:**
   ```bash
   docker-compose ps backend
   ```

2. **API работает:**
   ```bash
   curl "https://app.gstdtoken.com/api/v1/wallet/gstd-balance?address=EQD..."
   ```

3. **Логи:**
   ```bash
   docker-compose logs backend | grep -i "ton\|api"
   ```

## ⚠️ Важно

- **Не коммитить .env**: Файл `.env` должен быть в `.gitignore`
- **Лимит соблюдается автоматически**: Не нужно вручную контролировать
- **При превышении**: Запросы будут ждать доступных токенов

## 📚 Документация

- [TON_API_SETUP.md](./TON_API_SETUP.md) - детальная настройка
- TON API: https://tonapi.io/docs

