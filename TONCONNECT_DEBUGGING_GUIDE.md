# Руководство по отладке валидации TonConnect

## Обзор улучшений

После внедрения валидации TonConnect были добавлены улучшения для обеспечения 100% стабильности авторизации.

## 1. Подробное логирование

### Backend (`backend/internal/services/tonconnect_validator.go`)

Теперь валидатор выводит подробные логи на каждом этапе:

- **Начало валидации**: логирует wallet address, payload, signature (первые 20 символов)
- **Парсинг payload**: логирует успешный парсинг или ошибку формата
- **Проверка timestamp**: логирует текущее время, timestamp из payload, возраст подписи
- **Проверка nonce**: логирует наличие/отсутствие nonce
- **Проверка адреса**: логирует совпадение/несовпадение адресов
- **Декодирование подписи**: логирует формат (base64/hex) и длину
- **Получение публичного ключа**: логирует источник (frontend/TON API) и первые 16 символов
- **Верификация подписи**: логирует результат проверки Ed25519

**Примеры логов:**

```
🔐 TonConnect validation started for wallet: EQ...
📦 Payload received: {"nonce":"abc123","timestamp":1234567890,"address":"EQ..."}
✍️  Signature received (first 20 chars): abc123def456...
🔑 Public key provided: true
✅ Payload parsed successfully: nonce=abc123, timestamp=1234567890, address=EQ...
⏰ Timestamp validation: now=1234567890, payload_timestamp=1234567890, age=0s, maxAge=20m0s
✅ Signature decoded (base64): length=64 bytes
🔑 Using public key from frontend
✅ Public key from frontend: abc123def456... (first 16 chars)
🔐 Message hash computed: def456... (first 16 chars)
🔍 Verifying Ed25519 signature with public key from frontend...
✅ TonConnect signature validated successfully for wallet EQ...
```

**Ошибки логируются с префиксом ❌:**

```
❌ Expired timestamp: age 25m0s exceeds maximum 20m0s
❌ Invalid signature: Ed25519 verify returned false
❌ Public key not found: failed to resolve public key for wallet EQ...
❌ Address mismatch: expected EQ..., got UQ...
```

## 2. Получение публичного ключа

### Приоритет источников:

1. **Public key от фронтенда** (если предоставлен)
   - Извлекается из `signResult.publicKey` или `walletInfo.account.publicKey`
   - Преимущество: не требует запроса к TON API
   - Формат: hex или base64

2. **TON API** (fallback)
   - Используется, если public key не предоставлен фронтендом
   - Запрос к `tonapi.io/v2/accounts/{address}`
   - Кэшируется в Redis на 24 часа

### Backend изменения:

```go
func (v *TonConnectValidator) ValidateSignature(
    ctx context.Context,
    walletAddress string,
    signature string,
    payload string,
    maxAge time.Duration,
    publicKeyHex string, // Новый параметр
) error
```

### Frontend изменения:

Фронтенд теперь автоматически отправляет `public_key` в запросе, если доступен:

```typescript
const requestBody = {
  wallet_address: walletAddress,
  signature: signature,
  payload: payload,
  public_key: publicKey, // Опционально
};
```

## 3. Синхронизация времени

### Увеличенное окно валидности

**Было:** 10 минут  
**Стало:** 20 минут

Это решает проблемы с:
- Рассинхронизацией серверного времени
- Медленными сетями
- Задержками в обработке запросов

**Изменение в коде:**

```go
// routes_user.go
validator.ValidateSignature(ctx, walletAddress, req.Signature, req.Payload, 20*time.Minute, req.PublicKey)
```

## 4. Режим отладки (Development Mode)

### Переменная окружения: `SKIP_AUTH_VALIDATION`

Если установлена в `"true"`, валидация подписи пропускается.

**Использование:**

```bash
# В .env или docker-compose.yml
SKIP_AUTH_VALIDATION=true
```

**Важно:** Использовать только для разработки и тестирования, когда TON API недоступен.

**Логирование:**

```
⚠️  DEVELOPMENT MODE: Skipping TonConnect signature validation for wallet EQ...
```

## 5. Улучшенная обработка ошибок на фронтенде

### Детальное логирование ошибок 401

При ошибке 401 в консоль браузера выводится:

1. **Полный текст ошибки** от бэкенда
2. **Детали ошибки** (если доступны)
3. **Информация о запросе**:
   - wallet_address
   - payload
   - наличие signature
   - наличие public_key

**Пример вывода в консоли:**

```javascript
🔐 Authentication Error: signature validation failed: Invalid signature: Ed25519 verification failed
📋 Error Details: Invalid signature: Ed25519 verification failed
📦 Request Payload: {
  wallet_address: "EQ...",
  payload: "{\"nonce\":\"...\",\"timestamp\":...}",
  has_signature: true,
  has_public_key: true
}
```

### Проверка формата JSON

Фронтенд теперь логирует полную информацию о запросе перед отправкой:

```typescript
logger.debug('Sending login request', {
  url: `${apiBase}/api/v1/users/login`,
  wallet_address: walletAddress,
  has_signature: !!signature,
  has_public_key: !!publicKey,
  payload_length: payload.length,
});
```

## Отладка проблем

### Проблема: "Expired timestamp"

**Причина:** Подпись старше 20 минут или серверное время рассинхронизировано

**Решение:**
1. Проверить логи бэкенда для точного возраста подписи
2. Проверить синхронизацию времени сервера (NTP)
3. Убедиться, что фронтенд использует актуальное время

### Проблема: "Invalid signature"

**Причина:** Подпись не соответствует payload или публичному ключу

**Решение:**
1. Проверить логи бэкенда для деталей верификации
2. Убедиться, что payload на фронтенде и бэкенде идентичен
3. Проверить, что используется правильный публичный ключ
4. Убедиться, что хеш вычисляется одинаково (SHA-256)

### Проблема: "Public key not found"

**Причина:** TON API недоступен или не возвращает публичный ключ

**Решение:**
1. Использовать режим отладки (`SKIP_AUTH_VALIDATION=true`) для тестирования
2. Убедиться, что фронтенд отправляет `public_key` в запросе
3. Проверить доступность TON API
4. Проверить логи бэкенда для деталей запроса к API

### Проблема: "Address mismatch"

**Причина:** Адрес в payload не совпадает с адресом в запросе

**Решение:**
1. Проверить, что фронтенд использует правильный адрес кошелька
2. Убедиться, что адрес нормализован одинаково на фронтенде и бэкенде

## Тестирование

### 1. Нормальный сценарий

```bash
# Backend логи должны показать:
✅ Payload parsed successfully
✅ Signature decoded
✅ Public key from frontend (или TON API)
✅ TonConnect signature validated successfully
```

### 2. Тестирование с отключенным TON API

```bash
# Установить переменную окружения
export SKIP_AUTH_VALIDATION=true

# Backend логи должны показать:
⚠️  DEVELOPMENT MODE: Skipping TonConnect signature validation
```

### 3. Тестирование с истекшей подписью

```bash
# Backend логи должны показать:
❌ Expired timestamp: age 25m0s exceeds maximum 20m0s
```

## Мониторинг

Рекомендуется мониторить логи бэкенда на наличие:
- ❌ Ошибок валидации
- ⚠️  Предупреждений о недоступности TON API
- 🔑 Источников публичных ключей (frontend vs TON API)

## Безопасность

⚠️ **Важно:**
- Режим `SKIP_AUTH_VALIDATION=true` должен использоваться ТОЛЬКО в development окружении
- В production всегда должна быть включена валидация
- Публичные ключи от фронтенда должны проверяться на соответствие адресу кошелька
