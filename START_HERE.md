# 🚀 Инструкция по запуску

## ✅ Все проблемы исправлены

1. ✅ **Backend собирается** - go.sum обновлён, ошибки компиляции исправлены
2. ✅ **Порт 80 свободен** - системный Nginx остановлен
3. ✅ **Nginx конфигурация** - исправлена для работы без frontend/backend при получении SSL
4. ✅ **Redis функции** - исправлены методы на функции

## 📋 Быстрый запуск

### Шаг 1: Подготовка

```bash
# Убедиться что порт 80 свободен
./scripts/fix-nginx-port.sh

# Проверить что go.sum актуален (уже обновлён)
cd backend
docker run --rm -v "$(pwd)":/app -w /app golang:1.21-alpine sh -c "go mod download && go mod tidy"
cd ..
```

### Шаг 2: Получение SSL сертификата

```bash
# Запустить скрипт получения SSL
# Он автоматически использует упрощённую конфигурацию Nginx
./scripts/init-letsencrypt.sh
```

**Важно**: Скрипт автоматически:
- Использует упрощённую конфигурацию для получения SSL
- Запускает только Nginx
- Получает сертификат
- Восстанавливает полную конфигурацию

### Шаг 3: Запуск всех сервисов

```bash
# Запустить все сервисы с пересборкой
docker-compose up -d --build

# Проверить статус
docker-compose ps
```

### Шаг 4: Проверка работы

```bash
# Проверить что все сервисы запущены
docker-compose ps

# Проверить логи
docker-compose logs -f

# Тест сайта
curl -I https://app.gstdtoken.com

# Проверить API
curl https://app.gstdtoken.com/api/v1/stats
```

## 🔧 Если что-то пошло не так

### Backend не собирается

```bash
# Пересобрать go.sum
cd backend
docker run --rm -v "$(pwd)":/app -w /app golang:1.21-alpine sh -c "go mod download && go mod tidy"
cd ..

# Пересобрать backend
docker-compose build --no-cache backend
```

### Nginx не запускается

```bash
# Проверить конфигурацию
docker-compose run --rm --no-deps nginx nginx -t

# Проверить логи
docker-compose logs nginx

# Остановить и перезапустить
docker-compose stop nginx
docker-compose up -d nginx
```

### SSL сертификат не получается

```bash
# Проверить DNS
dig app.gstdtoken.com

# Проверить что порт 80 открыт
sudo ss -tlnp | grep :80

# Попробовать получить вручную
docker-compose run --rm --entrypoint "\
  certbot certonly --webroot -w /var/www/certbot \
    --email goldenbit.kz@yandex.kz \
    -d app.gstdtoken.com \
    --rsa-key-size 4096 \
    --agree-tos \
    --non-interactive \
    --verbose" certbot
```

## 📝 Полезные команды

```bash
# Статус всех сервисов
docker-compose ps

# Логи всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f nginx

# Перезапуск сервиса
docker-compose restart backend

# Остановка всех сервисов
docker-compose down

# Полная перезагрузка
docker-compose down
docker-compose up -d --build
```

## 🎯 Ожидаемый результат

После успешного запуска:

1. ✅ Все контейнеры в статусе "Up"
2. ✅ https://app.gstdtoken.com открывается
3. ✅ SSL сертификат валиден
4. ✅ API отвечает: https://app.gstdtoken.com/api/v1/stats
5. ✅ Frontend доступен на главной странице

## 📚 Дополнительная документация

- [QUICK_START.md](./QUICK_START.md) - Быстрый старт
- [DEPLOYMENT.md](./DEPLOYMENT.md) - Полная инструкция по развёртыванию
- [TROUBLESHOOTING.md](./TROUBLESHOOTING.md) - Решение проблем
- [ISSUES_FIXED.md](./ISSUES_FIXED.md) - Исправленные проблемы


