# Отладка проблемы с TonConnect QR-кодом

## Что изменено

### 1. Улучшена обработка подключения
- Используется `useTonConnect` hook для отслеживания состояния
- Добавлена обработка ошибок
- Добавлено детальное логирование

### 2. Улучшен UI
- Показывается статус подключения
- Отображаются ошибки
- Показывается адрес кошелька при подключении

### 3. Добавлено логирование
Все события логируются в консоль:
- `🔧 TonConnectUI initialized` - инициализация
- `🔌 Opening TonConnect modal...` - открытие модального окна
- `✅ TonConnect account connected` - успешное подключение
- `❌ Error` - ошибки

## Как проверить

1. **Откройте консоль браузера (F12)**
2. **Обновите страницу (Ctrl+F5)**
3. **Проверьте логи:**
   - Должно появиться: `🔧 TonConnectUI initialized`
   - Должно показать: `📱 Connected: false` (если не подключено)
4. **Нажмите "Подключить кошелек"**
   - Должно появиться: `🔌 Opening TonConnect modal...`
   - Должно появиться: `✅ Modal opened`
5. **Отсканируйте QR-код**
   - Должно появиться: `✅ TonConnect account connected`

## Возможные проблемы

### Проблема 1: Modal не открывается
**Симптомы:** Нет QR-кода, ошибка в консоли
**Решение:**
- Проверьте, что manifest.json доступен
- Проверьте консоль на ошибки CORS
- Убедитесь, что используется HTTPS

### Проблема 2: QR-код появляется, но ничего не происходит после сканирования
**Симптомы:** QR-код виден, но подключение не происходит
**Решение:**
- Проверьте, что кошелек поддерживает TonConnect
- Убедитесь, что используете правильную сеть (mainnet)
- Попробуйте другой кошелек (Tonkeeper, MyTonWallet)

### Проблема 3: Ошибка "TonConnectUI не инициализирован"
**Симптомы:** Кнопка неактивна, сообщение об ошибке
**Решение:**
- Обновите страницу (Ctrl+F5)
- Проверьте консоль на ошибки загрузки
- Проверьте, что manifest.json доступен

## Проверка manifest.json

```bash
curl https://app.gstdtoken.com/tonconnect-manifest.json
```

Должен вернуть JSON с полями:
- `url`
- `name`
- `iconUrl`
- `termsOfUseUrl`
- `privacyPolicyUrl`

## Проверка в консоли браузера

Откройте DevTools (F12) → Console и проверьте:

1. **Ошибки загрузки:**
   - Красные сообщения об ошибках
   - Ошибки CORS
   - Ошибки сети

2. **Логи TonConnect:**
   - `TonConnectUI initialized`
   - `Opening TonConnect modal...`
   - `TonConnect account connected`

3. **Проверка объекта:**
   ```javascript
   // В консоли браузера выполните:
   window.tonConnectUI
   ```
   Должен вернуть объект TonConnectUI

## Следующие шаги

Если проблема сохраняется:
1. Скопируйте все ошибки из консоли
2. Проверьте Network tab в DevTools
3. Попробуйте другой кошелек
4. Проверьте версию кошелька

