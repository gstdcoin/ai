# 🚀 MAINNET DEPLOYMENT INSTRUCTIONS

**Важно**: Для деплоя на mainnet нужно использовать флаг `--mainnet`!

---

## 📋 ПРАВИЛЬНАЯ КОМАНДА ДЕПЛОЯ

```bash
cd /home/ubuntu/gstd-contracts
npm run build
# Выберите: EscrowComplete

npx blueprint run deployEscrowMainnet --mainnet
```

**⚠️ КРИТИЧЕСКИ ВАЖНО**: 
- Используйте флаг `--mainnet` для принудительного деплоя на mainnet
- Убедитесь, что кошелек (Tonkeeper) подключен к **mainnet** (не testnet!)

---

## ⚠️ ПРОБЛЕМА, КОТОРУЮ МЫ ОБНАРУЖИЛИ

В предыдущей попытке деплоя транзакция была отправлена на **testnet** (`"network":"-239"`), хотя в `blueprint.config.ts` указан `type: 'mainnet'`.

### Причина:
TonConnect определяет сеть по кошельку пользователя. Если кошелек подключен к testnet, транзакция пойдет на testnet.

### Решение:
1. **Используйте флаг `--mainnet`** в команде деплоя
2. **Убедитесь, что кошелек подключен к mainnet**

---

## ✅ ПРОВЕРКА ПЕРЕД ДЕПЛОЕМ

### 1. Проверьте сеть кошелька:
- Откройте Tonkeeper
- Проверьте настройки сети
- Должно быть: **Mainnet** (не Testnet)

### 2. Проверьте конфигурацию:
- `blueprint.config.ts` должен содержать `type: 'mainnet'`
- ✅ Уже настроено правильно

### 3. Используйте правильную команду:
```bash
npx blueprint run deployEscrowMainnet --mainnet
```

---

## 📊 ПОСЛЕ ДЕПЛОЯ

### Проверьте контракт:
- **Mainnet**: https://tonscan.org/address/<АДРЕС>
- **Testnet**: https://testnet.tonscan.org/address/<АДРЕС>

### Обновите конфигурацию:
После успешного деплоя обновите `docker-compose.yml`:
```yaml
backend:
  environment:
    - TON_CONTRACT_ADDRESS=<НОВЫЙ_АДРЕС_MAINNET>
```

---

**Дата**: 2025-01-10  
**Статус**: ✅ **ГОТОВО К ПОВТОРНОМУ ДЕПЛОЮ С ФЛАГОМ --mainnet**
