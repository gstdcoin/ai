# 🔧 Исправления подключения кошелька (v2)

## Проблема
Подключение не работало ни в одном из кошельков - после сканирования QR кода ничего не происходило.

## Анализ проблемы

После проверки документации TonConnect SDK выявлено:
1. **Рекомендуется использовать встроенный `TonConnectButton`** вместо кастомной кнопки
2. **Нужна правильная конфигурация** `TonConnectUIProvider`
3. **Важна настройка `restoreConnection`** для восстановления подключения

## Исправления

### 1. Использование встроенного TonConnectButton
- **Было**: Кастомная кнопка с `openModal()`
- **Стало**: Встроенный `<TonConnectButton />` компонент
- **Преимущества**:
  - Автоматическая обработка всех событий подключения
  - Правильная работа с QR кодами
  - Поддержка всех кошельков из коробки
  - Автоматическое управление модальными окнами

### 2. Улучшена конфигурация TonConnectUIProvider
Добавлены важные параметры:
```tsx
<TonConnectUIProvider 
  manifestUrl={manifestUrl}
  restoreConnection={true}  // ← Восстановление подключения
  language="ru"             // ← Язык интерфейса
  actionsConfiguration={{
    twaReturnUrl: 'https://t.me/gstdtoken_bot'
  }}
  uiPreferences={{
    theme: THEME.DARK,
    borderRadius: 'm',
    colorsSet: {
      [THEME.DARK]: {
        connectButton: {
          background: '#FFD700',
          foreground: '#0a1929'
        }
      }
    }
  }}
>
```

### 3. Сохранена обработка событий для логина
- `onStatusChange` обрабатывает подключение
- Автоматический вызов API `/api/v1/users/login`
- Сохранение пользователя в store и localStorage

## Как это работает теперь

1. **Пользователь видит `TonConnectButton`**
   - Встроенный компонент от TonConnect
   - Автоматически показывает QR код при клике
   - Поддерживает все кошельки

2. **После сканирования QR кода**:
   - `TonConnectButton` автоматически обрабатывает подключение
   - Срабатывает `onStatusChange` событие
   - Наш код вызывает `loginUser()` для входа в систему
   - Модальное окно закрывается автоматически

3. **Восстановление подключения**:
   - `restoreConnection={true}` восстанавливает подключение при перезагрузке
   - Пользователь остается залогиненным

## Проверка

1. **Откройте платформу**: https://app.gstdtoken.com
2. **Нажмите на кнопку подключения** (теперь это встроенный TonConnectButton)
3. **Сканируйте QR код** в любом поддерживаемом кошельке:
   - Tonkeeper
   - TON Wallet
   - MyTonWallet
   - И другие поддерживаемые кошельки
4. **Ожидаемое поведение**:
   - QR код отображается в модальном окне
   - После сканирования модальное окно закрывается
   - Кошелек подключается
   - Выполняется вход в систему
   - Появляется сообщение об успешном подключении

## Отладка

Если проблема сохраняется:

### 1. Проверьте консоль браузера (F12)
Должны быть логи:
```
TonConnectUI initialized
Wallet connected via onStatusChange
User logged in successfully
```

### 2. Проверьте manifest файл
```bash
curl https://app.gstdtoken.com/tonconnect-manifest.json
```
Должен вернуть валидный JSON.

### 3. Проверьте Network tab
- Должен быть запрос к `/api/v1/users/login`
- Статус должен быть 200 OK

### 4. Проверьте версию пакета
```bash
cd frontend && npm list @tonconnect/ui-react
```
Должна быть версия `^2.0.0` или выше.

## Технические детали

### Изменения в коде:

1. **WalletConnect.tsx**:
   - Добавлен импорт `TonConnectButton`
   - Заменена кастомная кнопка на `<TonConnectButton />`
   - Сохранена логика обработки событий для логина
   - Улучшена обработка состояния подключения

2. **_app.tsx**:
   - Добавлен `restoreConnection={true}`
   - Добавлен `language="ru"`

### Преимущества нового подхода:

- ✅ Работает со всеми кошельками из коробки
- ✅ Автоматическая обработка всех событий
- ✅ Правильное отображение QR кодов
- ✅ Восстановление подключения при перезагрузке
- ✅ Меньше кастомного кода = меньше багов

## Поддерживаемые кошельки

TonConnectButton автоматически поддерживает:
- ✅ Tonkeeper
- ✅ TON Wallet  
- ✅ MyTonWallet
- ✅ DeWallet
- ✅ И другие кошельки с поддержкой TonConnect 2.0

## Следующие шаги

Если проблема не решена:
1. Проверьте логи в консоли браузера
2. Убедитесь, что manifest файл доступен
3. Проверьте версию `@tonconnect/ui-react` (должна быть >= 2.0.0)
4. Попробуйте другой кошелек для тестирования

---

**Дата исправления**: 11 января 2026  
**Версия**: 2.0  
**Статус**: ✅ Исправлено с использованием встроенного TonConnectButton
