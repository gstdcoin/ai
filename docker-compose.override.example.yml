# Copy to docker-compose.override.yml (gitignored) for local-only bind mounts.
# Example: mount host repo so operator tools inside backend see the workspace.
#
# cp docker-compose.override.example.yml docker-compose.override.yml
#
# version: "3.8"
# services:
#   backend-blue:
#     volumes:
#       - ${GSTD_HOST_REPO:-.}:/workspace:rw

services: {}
