# 🔍 ПОЛНЫЙ ОТЧЕТ ДИАГНОСТИКИ ПЛАТФОРМЫ GSTD

**Дата:** 2026-01-11  
**Статус:** ✅ Все компоненты работают, но есть проблемы с UX входа

---

## 📊 1. ПРОБЛЕМА ВХОДА В СИСТЕМУ

### ✅ Что работает:
- **Backend API:** `/api/v1/users/login` работает корректно (200 OK)
- **TonConnect:** Инициализируется правильно
- **База данных:** Подключение стабильное

### ⚠️ Проблема:
**Проблема не в коде, а в UX (User Experience):**

1. **TonConnect требует времени на инициализацию** (2 секунды по коду)
2. **Пользователь видит "TonConnect не готов"** даже когда все работает
3. **После подключения кошелька** `isConnected` устанавливается, но Dashboard может не показаться сразу

### 🔧 Решение:
Проблема уже частично исправлена добавлением `isInitializing` состояния. Нужно убедиться, что:
- Пользователь видит индикатор загрузки
- После подключения кошелька Dashboard показывается автоматически
- Логин API вызывается и `setUser` устанавливает данные пользователя

---

## 📋 2. КАК СОЗДАЕТСЯ ЗАДАЧА

### Процесс создания задачи:

#### Шаг 1: Пользователь заполняет форму
- **Frontend:** `NewTaskModal.tsx`
- **Поля:** `type`, `budget` (GSTD), `payload` (JSON, опционально)

#### Шаг 2: Проверка баланса GSTD
- **Endpoint:** `/api/v1/wallet/gstd-balance?address={address}`
- **Минимум:** 0.000001 GSTD
- **Если баланс недостаточен:** Показывается ошибка

#### Шаг 3: Создание задачи
- **Endpoint:** `POST /api/v1/tasks/create?wallet_address={address}`
- **Backend:** `TaskPaymentService.CreateTask()`
- **Статус:** `pending_payment`
- **Генерируется:**
  - `task_id` (UUID)
  - `payment_memo` (формат: `TASK-{task_id}`)
  - `platform_wallet` (адрес для оплаты)
  - `amount` (бюджет в GSTD)

#### Шаг 4: Оплата задачи
- **Заказчик отправляет GSTD токены:**
  - **На адрес:** `platform_wallet` (из ответа API)
  - **Сумма:** `amount` GSTD
  - **В комментарии (memo):** `payment_memo` (например: `TASK-123e4567-e89b-12d3-a456-426614174000`)

#### Шаг 5: Подтверждение оплаты
- **Сервис:** `PaymentWatcher` отслеживает входящие транзакции
- **Проверка:** Ищет GSTD transfers с нужным `payment_memo`
- **Обновление:** Статус задачи меняется на `queued`
- **Frontend:** Опрашивает `/api/v1/tasks/{task_id}/payment` каждые 5 секунд

---

## 💰 3. ЧТО СЛУЖИТ НАГРАДОЙ ЗА ВЫПОЛНЕНИЕ

### Система наград:

#### Награда работника (Executor):
- **Поле в БД:** `labor_compensation_ton` (в TON)
- **Расчет:** 
  - Бюджет задачи: `budget_gstd` (в GSTD)
  - Платформа берет: 5% (`platform_fee`)
  - Работник получает: 95% (`reward_gstd` = `budget_gstd * 0.95`)
- **Выплата:** Через escrow контракт (pull-model)

#### Pull-Model (Работник сам забирает награду):
1. Работник выполняет задачу
2. Результат валидируется
3. Работник получает `payout_intent` через API
4. Работник подписывает транзакцию через TonConnect
5. Escrow контракт отправляет награду работнику
6. Платформа автоматически получает 5% комиссию

#### Escrow контракт:
- **Адрес:** `EQAIYlrr3UiMJ9fqI-B4j2nJdiiD7WzyaNL1MX_wiONc4OUi` (mainnet)
- **Функция:** Хранит средства до выполнения задачи
- **Выплата:** Работник сам инициирует через TonConnect

---

## 💳 4. КАК ПОПОЛНЯТЬ НАГРАДУ ЗАКАЗЧИКУ

### Процесс пополнения escrow:

#### Вариант 1: Через веб-интерфейс (текущий)
1. **Создать задачу** через `NewTaskModal`
2. **Получить данные для оплаты:**
   - `platform_wallet` (адрес платформы)
   - `amount` (сумма в GSTD)
   - `payment_memo` (уникальный идентификатор)
3. **Отправить GSTD токены:**
   - Открыть TON кошелек (Tonkeeper, MyTonWallet и т.д.)
   - Отправить `amount` GSTD на `platform_wallet`
   - **ВАЖНО:** Включить `payment_memo` в комментарий транзакции
4. **Подтвердить оплату:**
   - Нажать "Confirm Payment" в интерфейсе
   - Система автоматически обнаружит платеж через `PaymentWatcher`

#### Вариант 2: Прямая отправка (без интерфейса)
1. Отправить GSTD на адрес платформы
2. Включить `payment_memo` в комментарий
3. Система автоматически обнаружит платеж

### ⚠️ ВАЖНО:
- **Без `payment_memo`** платеж не будет привязан к задаче
- **Платеж должен быть в GSTD токенах**, не в TON
- **Минимальная сумма:** Зависит от задачи (обычно от 0.000001 GSTD)

---

## 🔍 5. ДЕТАЛЬНАЯ ДИАГНОСТИКА ВХОДА

### Проверка компонентов:

#### ✅ Backend:
```bash
curl -X POST http://localhost:8080/api/v1/users/login \
  -H "Content-Type: application/json" \
  -d '{"wallet_address":"EQAIYlrr3UiMJ9fqI-B4j2nJdiiD7WzyaNL1MX_wiONc4OUi"}'
# Ответ: 200 OK с данными пользователя
```

#### ✅ Frontend:
- `WalletConnect.tsx` вызывает `loginUser()` после подключения кошелька
- `loginUser()` вызывает `/api/v1/users/login`
- `setUser()` сохраняет данные пользователя
- `connect()` устанавливает `isConnected = true`

#### ⚠️ Потенциальная проблема:
В `index.tsx` проверяется `isConnected`, но может быть задержка между:
1. Подключением кошелька (TonConnect)
2. Вызовом `loginUser()` (API)
3. Установкой `isConnected` (Zustand store)

### 🔧 Рекомендации:
1. Добавить более явную индикацию загрузки
2. Убедиться, что Dashboard показывается сразу после `isConnected = true`
3. Добавить логирование для отладки

---

## 📊 6. СТРУКТУРА БАЗЫ ДАННЫХ

### Таблица `tasks`:
- `task_id` - UUID задачи
- `creator_wallet` - Кошелек создателя
- `requester_address` - Адрес заказчика
- `budget_gstd` - Бюджет в GSTD
- `reward_gstd` - Награда работника (95% от бюджета)
- `labor_compensation_ton` - Компенсация труда в TON
- `payment_memo` - Уникальный идентификатор платежа
- `status` - Статус задачи (`pending_payment`, `queued`, `assigned`, `completed`)
- `escrow_status` - Статус escrow (`pending`, `locked`, `distributed`)

---

## ✅ ЗАКЛЮЧЕНИЕ

### Все компоненты работают правильно:
1. ✅ Backend API функционирует
2. ✅ База данных подключена
3. ✅ TonConnect инициализируется
4. ✅ Система создания задач работает
5. ✅ Система оплаты настроена
6. ✅ Escrow контракт развернут

### Проблема входа:
**Не критическая** - это проблема UX, а не функциональности. Система работает, но пользователь может видеть "TonConnect не готов" в течение 2 секунд после загрузки страницы.

### Рекомендации:
1. Улучшить индикацию загрузки
2. Добавить более понятные сообщения для пользователя
3. Убедиться, что Dashboard показывается сразу после подключения

---

**Отчет подготовлен:** AI Assistant  
**Дата:** 2026-01-11
