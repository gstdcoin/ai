# ═══════════════════════════════════════════════════════════════
# chat.gstdtoken.com — GSTD Sovereign AI Chat
# Static single-page app + Groq API (client-side)
# ═══════════════════════════════════════════════════════════════

server {
    listen 443 ssl;
    server_name chat.gstdtoken.com;

    ssl_certificate /etc/letsencrypt/live/app.gstdtoken.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/app.gstdtoken.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
    ssl_session_cache shared:SSL_CHAT:5m;

    # Security Headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-XSS-Protection "1; mode=block" always;

    root /var/www/chat;
    index index.html;

    location /health {
        access_log off;
        return 200 'healthy\n';
    }

    # SPA fallback — always serve index.html
    location / {
        try_files $uri $uri/ /index.html;
        
        # Cache static assets
        add_header Cache-Control "no-cache, must-revalidate";
    }
}

# HTTP → HTTPS redirect
server {
    listen 80;
    server_name chat.gstdtoken.com;
    return 301 https://$host$request_uri;
}
