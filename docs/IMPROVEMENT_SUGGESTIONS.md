# GSTD Platform — Предложения по улучшению

**Дата:** 2026-02-11

## 1. Миграции БД

### Проблема: дублирование версий
В `backend/migrations/` есть файлы с одинаковыми префиксами версий:
- v16, v17, v19, v22, v23, v39, v48, v49 — по 2–3 файла на версию

**Рекомендация:** Переименовать в уникальную последовательность (v52, v53, …) и обновить скрипты миграций.

---

## 2. Логирование

### Проблема: неструктурированные логи
Используется `log.Printf` по всему backend.

**Рекомендация:** Внедрить zerolog или zap для структурированного логирования.

---

## 3. Frontend — TypeScript

### Проблема: `ignoreBuildErrors: true` в next.config.js

**Рекомендация:** Включить проверку и исправить ошибки.

---

## 4. Тесты

**Рекомендация:** Unit-тесты для TaskService, RewardEngine, EscrowService; интеграционные с testcontainers.

---

## 5. Документация

**Рекомендация:** Консолидировать 70+ MD файлов; устаревшие аудиты в docs/archive/.

---

## 6. Скрипты

**Рекомендация:** deploy.sh в корне и scripts/deploy.sh — оставить один, убрать дублирование.
