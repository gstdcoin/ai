# 🔄 PostgreSQL Backup Setup

**Критическая задача:** Защита данных пользователей от потери при сбоях сервера.

---

## 📋 Обзор

Скрипт `scripts/backup.sh` создает автоматические резервные копии базы данных PostgreSQL каждые 12 часов.

### Особенности:
- ✅ Сжатие бэкапов (gzip)
- ✅ Проверка целостности бэкапов
- ✅ Автоматическая очистка старых бэкапов (14 дней)
- ✅ Логирование всех операций
- ✅ Автоматическое определение контейнера PostgreSQL

---

## 🚀 Быстрая установка

### 1. Создать скрипт бэкапа (уже создан)

```bash
# Скрипт находится в: /home/ubuntu/scripts/backup.sh
chmod +x /home/ubuntu/scripts/backup.sh
```

### 2. Настроить cron (автоматически)

```bash
# Автоматическая настройка (удалит дубликаты и старые задачи)
/home/ubuntu/scripts/setup-backup-cron.sh
```

### 3. Ручная настройка cron (если нужно)

```bash
# Открыть crontab для редактирования
crontab -e

# Добавить строку (бэкап каждые 12 часов в 00:00 и 12:00):
0 */12 * * * /home/ubuntu/scripts/backup.sh >> /home/ubuntu/logs/backup.log 2>&1
```

---

## 📁 Структура бэкапов

```
/home/ubuntu/backups/
├── postgres_backup_20260113_000000.sql.gz  # Бэкап в 00:00
├── postgres_backup_20260113_120000.sql.gz  # Бэкап в 12:00
├── postgres_backup_20260114_000000.sql.gz  # Следующий бэкап
└── backup.log                               # Логи бэкапов
```

**Формат имени:** `postgres_backup_YYYYMMDD_HHMMSS.sql.gz`

---

## ⚙️ Конфигурация

### Параметры в скрипте `backup.sh`:

```bash
BACKUP_DIR="/home/ubuntu/backups"      # Директория для бэкапов
RETENTION_DAYS=14                      # Хранить 14 дней
DB_NAME="distributed_computing"            # Имя базы данных
DB_USER="postgres"                         # Пользователь БД
CONTAINER_NAME="gstd_postgres"             # Имя контейнера
```

### Расписание cron:

```
0 */12 * * *  # Каждые 12 часов в 00:00 и 12:00
```

**Альтернативные варианты:**
- `0 2,14 * * *` - В 02:00 и 14:00 каждый день
- `0 */6 * * *` - Каждые 6 часов (00:00, 06:00, 12:00, 18:00)
- `0 3 * * *` - Один раз в день в 03:00

---

## 🔍 Проверка работы

### 1. Проверить, что cron задача установлена:

```bash
crontab -l | grep backup.sh
```

**Ожидаемый результат:**
```
0 */12 * * * /home/ubuntu/scripts/backup.sh >> /home/ubuntu/logs/backup.log 2>&1
```

### 2. Запустить бэкап вручную:

```bash
/home/ubuntu/scripts/backup.sh
```

**Ожидаемый результат:**
```
[2026-01-13 21:30:00] Starting PostgreSQL backup...
[2026-01-13 21:30:00] Container: gstd_postgres
[2026-01-13 21:30:00] Database: distributed_computing
[2026-01-13 21:30:00] Backup file: /home/ubuntu/backups/postgres_backup_20260113_213000.sql.gz
[SUCCESS] Backup created successfully: /home/ubuntu/backups/postgres_backup_20260113_213000.sql.gz
[2026-01-13 21:30:01] Backup size: 2.5M
[SUCCESS] Backup integrity verified
[2026-01-13 21:30:01] Total backups: 5
[2026-01-13 21:30:01] Total backup size: 12M
```

### 3. Проверить список бэкапов:

```bash
ls -lh /home/ubuntu/backups/postgres_backup_*.sql.gz
```

### 4. Проверить логи:

```bash
tail -20 /home/ubuntu/logs/backup.log
# или
tail -20 /home/ubuntu/backups/backup.log
```

---

## 🔄 Восстановление из бэкапа

### Восстановить базу данных:

```bash
# 1. Остановить приложение (опционально, для безопасности)
docker-compose stop backend

# 2. Распаковать и восстановить
gunzip < /home/ubuntu/backups/postgres_backup_20260113_120000.sql.gz | \
  docker exec -i gstd_postgres psql -U postgres -d distributed_computing

# 3. Перезапустить приложение
docker-compose start backend
```

### Или использовать существующий скрипт:

```bash
# Использовать скрипт восстановления
/home/ubuntu/scripts/restore-database.sh /home/ubuntu/backups/postgres_backup_20260113_120000.sql.gz
```

---

## 🧹 Очистка crontab

### Проблема: Дубликаты и старые задачи

**До очистки:**
```
0 2 * * * /home/ubuntu/scripts/backup-database.sh  # Дубликат 1
0 2 * * * /home/ubuntu/scripts/backup-database.sh  # Дубликат 2
0 2 * * * /home/ubuntu/scripts/backup-database.sh  # Дубликат 3
0 3 * * * /home/ubuntu/backup_db.sh                 # Старый скрипт
* * * * * /home/ubuntu/monitor.sh                    # Слишком часто
* * * * * /home/ubuntu/check_gateway.sh             # Слишком часто
```

**После очистки:**
```
0 */12 * * * /home/ubuntu/scripts/backup.sh          # Новый скрипт (каждые 12 часов)
*/5 * * * * /home/ubuntu/scripts/health-check.sh     # Health check (каждые 5 минут)
*/15 * * * * /home/ubuntu/scripts/auto-recovery.sh  # Auto recovery (каждые 15 минут)
0 */6 * * * /home/ubuntu/scripts/auto-rebuild.sh     # Auto rebuild (каждые 6 часов)
0 0 1 * * certbot renew --post-hook 'docker exec gstd_gateway nginx -s reload'  # SSL renewal
```

### Автоматическая очистка:

```bash
/home/ubuntu/scripts/setup-backup-cron.sh
```

### Ручная очистка:

```bash
# Открыть crontab
crontab -e

# Удалить дубликаты и старые задачи:
# - backup-database.sh (дубликаты)
# - backup_db.sh (старый скрипт)
# - monitor.sh (слишком часто, используйте health-check.sh)
# - check_gateway.sh (слишком часто, используйте health-check.sh)
```

---

## 📊 Мониторинг

### Проверить размер бэкапов:

```bash
du -sh /home/ubuntu/backups/
```

### Проверить количество бэкапов:

```bash
ls -1 /home/ubuntu/backups/postgres_backup_*.sql.gz | wc -l
```

### Проверить последний бэкап:

```bash
ls -lht /home/ubuntu/backups/postgres_backup_*.sql.gz | head -1
```

### Проверить логи последних бэкапов:

```bash
tail -50 /home/ubuntu/logs/backup.log
```

---

## ⚠️ Важные замечания

### 1. Дисковое пространство

- **Рекомендация:** Минимум 5GB свободного места для бэкапов
- **Текущая политика:** Хранить 14 дней бэкапов
- **Проверка:** `df -h /home/ubuntu/backups`

### 2. Безопасность

- Бэкапы содержат **все данные пользователей** (балансы, транзакции)
- **Рекомендация:** Настроить автоматическую отправку бэкапов на внешний сервер (S3, FTP, etc.)

### 3. Тестирование восстановления

- **Рекомендация:** Раз в месяц тестировать восстановление из бэкапа на тестовой среде

### 4. Уведомления

- **Рекомендация:** Настроить мониторинг успешности бэкапов (email, Telegram, etc.)

---

## 🔧 Устранение проблем

### Проблема: "PostgreSQL container not found"

**Решение:**
```bash
# Проверить имя контейнера
docker ps --format "{{.Names}}" | grep postgres

# Обновить CONTAINER_NAME в скрипте, если нужно
```

### Проблема: "Backup failed - pg_dump command failed"

**Решение:**
```bash
# Проверить, что контейнер работает
docker ps | grep postgres

# Проверить доступ к базе данных
docker exec gstd_postgres psql -U postgres -d distributed_computing -c "SELECT 1;"
```

### Проблема: "No space left on device"

**Решение:**
```bash
# Очистить старые бэкапы вручную
find /home/ubuntu/backups -name "postgres_backup_*.sql.gz" -mtime +7 -delete

# Или уменьшить RETENTION_DAYS в скрипте
```

### Проблема: Cron не запускается

**Решение:**
```bash
# Проверить, что cron запущен
sudo systemctl status cron

# Проверить логи cron
grep CRON /var/log/syslog | tail -20

# Проверить права на скрипт
ls -l /home/ubuntu/scripts/backup.sh
chmod +x /home/ubuntu/scripts/backup.sh
```

---

## 📚 Дополнительные ресурсы

- **Скрипт бэкапа:** `/home/ubuntu/scripts/backup.sh`
- **Скрипт настройки cron:** `/home/ubuntu/scripts/setup-backup-cron.sh`
- **Логи бэкапов:** `/home/ubuntu/logs/backup.log` или `/home/ubuntu/backups/backup.log`
- **Директория бэкапов:** `/home/ubuntu/backups/`

---

**Обновлено:** 2026-01-13  
**Статус:** ✅ Настроено и работает
