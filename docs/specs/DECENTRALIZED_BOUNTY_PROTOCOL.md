# GSTD Decentralized Bounty Protocol (DBP)
### "Trustless Task Execution Standard"

## 1. Философия
Протокол DBP передает, полный контроль над рынком в руки участников. Бот выступает лишь как интерфейс и гарант соблюдения формата.
**Core Principle:** "Skin in the game". И заказчик (платит вперед), и исполнитель (рискует залогом) финансово мотивированы на честную сделку.

## 2. Команды "Командира" (Заказчика) через Бота

### Создание задачи
Команда: `/bounty [Reward_GSTD] [Task_Description]`
Пример: `/bounty 500 Создать 3D модель кроссовок Nike Air в формате .obj`

**Действия бота:**
1.  **Анализ:** AI парсит запрос, формализует требования (Формат: OBJ, Полигонов: <50k, Текстуры: 4k).
2.  **Блокирока:** Списывает 500 GSTD с кошелька пользователя в Smart Contract Escrow.
3.  **Публикация:** Задача появляется в глобальном фиде воркеров.

### Контроль статуса
Команда: `/my_tasks`
Интерфейс:
```
📦 Task #1024: 3D Model Nike
💰 Reward: 500 GSTD
🟢 Status: IN_PROGRESS
👷 Worker: node_x92 (Staked: 50 GSTD)
⏳ Deadline: 3h 12m
```

### Приемка работы
Когда воркер присылает результат, бот присылает уведомление.
Кнопки: `[✅ ACCPET & RELEASE]` | `[⚠️ REJECT & DISPUTE]`

## 3. Workflow Исполнителя (Worker Node)

### Job Feed (Лента заказов)
Воркеры (или их автоматические скрипты) мониторят ленту:
`Task #1024 | Type: 3D_GEN | Bounty: 500 GSTD | Collateral: 50 GSTD`

### Механизм "Insurance Stake" (Защита от дурака)
Чтобы нажать кнопку `Take Job`, воркер обязан заблокировать **10% от суммы награды** (Collateral) в смарт-контракте.
-   **Успех:** Воркер получает `500 + 50 = 550 GSTD`.
-   **Провал (Обман/Мусор/Просрочка):** Воркер теряет `50 GSTD`. Эти деньги сжигаются или отправляются заказчику как компенсация за ожидание.

## 4. Автономный Арбитраж (AI Validator)
Чтобы заказчик не мог обмануть воркера (получить результат и нажать Reject), в систему встроен AI-Арбитр.

Если Заказчик нажимает `REJECT`, включается AI-Трибунал:
1.  Система сравнивает **ТЗ** (текст заказчика) и **Результат** (файл воркера).
2.  **Сценарий A (Воркер схалтурил):** AI видит, что файл битый или не соответствует промпту. -> *Stake Burned, User Refunded.*
3.  **Сценарий B (Заказчик врет):** AI видит, что работа выполнена идеально по ТЗ. -> *Force Payout (деньги уходят воркеру принудительно).*

## 5. Формат Результата
Бот гарантирует, что "Результат придет именно в том виде, что просил заказчик", используя строгие схемы валидации *до* того, как показать результат юзеру.

Пример схемы (`task_schemas.yaml`):
```yaml
WEB_SCRAPING:
  required_file: .csv
  min_rows: 10
  required_columns: ["price", "url", "title"]
  forbidden_content: ["null", "error"]
```
Если воркер пришлет JSON вместо CSV или пустой файл, Бот **автоматически** отклонит сдачу, не беспокоя заказчика.

## 6. Итог
- **Заказчик:** Контролирует цену, защищен от спама (стейкингом воркера), получает валидированный результат.
- **Воркер:** Видит цену заранее, сам решает (рынок), защищен смарт-контрактом от неоплаты.
- **Платформа:** Получает комиссию и полезный объем торгов GSTD.
