# GSTD Platform — Full Audit Report

**Date:** 2026-02-21  
**Scope:** Backend, Frontend, Integrations, Modules, API consistency

---

## 1. Build & Tests

| Component | Status |
|-----------|--------|
| Backend (`go build ./...`) | ✅ OK |
| Frontend (`npm run build`) | ✅ OK |
| Backend tests (`go test ./...`) | ✅ OK |
| Linter | ✅ No errors |

---

## 2. Backend Services

### Core Services
- **PoolMonitorService** — GSTD price (Ston.fi, TON-GSTD fallback, golden_reserve_log, cache, config fallback)
- **StonFiService** — Pool data, GetPoolByMarket for TON-GSTD
- **TelegramService** — Webhook, Stars, Balance, Nodes, Buy GSTD
- **BitchatBridgeService** — Offline mesh bridge (skeleton, `BITCHAT_BRIDGE_ENABLED=1`)

### Integrations
- **TON** — Balance, Jetton, Contract
- **Ston.fi** — Pool, TON-GSTD price fallback
- **CoinGecko** — XAUt, TON price
- **Telegram** — Webhook, Stars, ReplyKeyboard

### Config
- **GSTDJettonAddress** — Default `EQDv6cYW9nNiKjN3Nwl8D6ABjUiH1gYfWVGZhfP7-9tZskTO`
- **GSTD_FALLBACK_PRICE_USD** — Default 0.02 when all sources fail

---

## 3. Frontend ↔ API

| Frontend | API | Status |
|----------|-----|--------|
| `/market/price` | `GET /market/price` | ✅ |
| `/network/stats` | `GET /network/stats` | ✅ |
| `/wallet/balance` | `GET /wallet/balance` | ✅ |
| `/users/balance` | `GET /users/balance` | ✅ |
| `/chat/completions` | `POST /chat/completions` | ✅ |
| `/chat/ultra-status` | `GET /chat/ultra-status` | ✅ |
| `/device/tasks/available` | `GET /device/tasks/available` | ✅ (fixed device_id) |
| `/nodes/my` | `GET /nodes/my` | ✅ |
| `/nodes/activate-wallet` | `POST /nodes/activate-wallet` | ✅ |
| `/marketplace/*` | Marketplace routes | ✅ |
| `/payments/payout-intent` | `POST /payments/payout-intent` | ✅ |
| `/tokens/*` | Onboarding tokens | ✅ |
| `/referrals/*` | Referral routes | ✅ |
| `/admin/architect/*` | Admin routes | ✅ |

---

## 4. Fixes Applied

### 4.1 GSTD Price
- **TON-GSTD fallback** — PoolMonitorService uses Ston.fi TON-GSTD pool when GSTD/XAUt fails
- **GetTONPriceUSD()` — CoinGecko for TON price
- **Config fallback** — `GSTD_FALLBACK_PRICE_USD` when all sources fail

### 4.2 Telegram Bot
- **getBotAPIToken()** — Uses `BOT_API_KEY` or `TELEGRAM_BOT_TOKEN` for internal API calls
- **Balance/Nodes** — Correct `telegram_id` in URL

### 4.3 Chat & Balance
- **ChatPanel** — Fetches balance on mount when `address` exists and `gstdBalance` is null
- **WalletListener** — Uses `/users/balance`; WalletBalanceWidget uses `/wallet/balance`

### 4.4 TasksPanel
- **device_id** — Uses `autonomous-${address.slice(0, 8)}` for `/device/tasks/available` (matches backend)

### 4.5 Bitchat Bridge
- **BitchatBridgeService** — Fetches queued tasks, builds `gstd:task`, placeholder for bitchat send
- **Start()** — Runs when `BITCHAT_BRIDGE_ENABLED=1`

---

## 5. Known TODOs (Non-blocking)

| Location | Description |
|----------|-------------|
| bitchat_bridge_service.go | Integrate with bitchat API/SDK |
| sovereign_bridge_service.go | STON.fi/DeDust API, refund logic |
| routes_bridge.go | Task status from Redis, escrow release |
| leviathan/hyper_sensors.go | External APIs (Hugging Face, GitHub, etc.) |
| constitution_anchor_service.go | TonWalletService.SendWithComment |

---

## 6. Environment Variables

| Variable | Purpose |
|----------|---------|
| `GSTD_JETTON_ADDRESS` | GSTD jetton (default set) |
| `GSTD_FALLBACK_PRICE_USD` | Fallback price (default 0.02) |
| `BOT_API_KEY` | Telegram bot API (optional, else TELEGRAM_BOT_TOKEN) |
| `API_PUBLIC_URL` | Backend URL for internal calls |
| `BITCHAT_BRIDGE_ENABLED` | Enable Bitchat Bridge (1) |

---

## 7. Summary

- **Build:** ✅ Backend and frontend build successfully
- **Tests:** ✅ All backend tests pass
- **Linter:** ✅ No errors
- **API:** ✅ Frontend ↔ API alignment verified
- **Integrations:** ✅ Telegram, Stars, TON, Ston.fi configured
- **Fixes:** ✅ Price fallback, device_id, balance fetch, Bitchat Bridge skeleton

---

*Report generated by platform audit*
