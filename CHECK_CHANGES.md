# Проверка изменений

## ✅ Что должно быть видно

### 1. Двуязычная поддержка
- **Переключатель языка** в Sidebar (кнопка с 🌐 и текстом "English" или "Русский")
- При переключении языка весь интерфейс должен меняться
- URL должен содержать `/ru` или `/en`

### 2. Переводы в таблице заданий
- Заголовок "Действия" (или "Actions" на английском)
- Текст "Нет заданий" (или "No tasks" на английском)

### 3. Проверка GSTD баланса
- API endpoint работает: `GET /api/v1/wallet/gstd-balance?address=<address>`
- При создании задания проверяется наличие GSTD

## 🔍 Как проверить

### Проверка переводов в браузере
1. Откройте https://app.gstdtoken.com
2. Подключите кошелёк
3. Перейдите в раздел "Задания"
4. Должны увидеть:
   - Заголовок колонки "Действия"
   - Если нет заданий - "Нет заданий"

### Проверка переключения языка
1. В Sidebar нажмите кнопку с 🌐
2. Интерфейс должен переключиться на английский
3. URL должен измениться на `/en`

### Проверка API
```bash
# Проверка GSTD баланса
curl "https://app.gstdtoken.com/api/v1/wallet/gstd-balance?address=EQD..."

# Проверка статистики
curl "https://app.gstdtoken.com/api/v1/stats"
```

## 🐛 Если не видно изменений

### 1. Очистить кэш браузера
- Нажмите Ctrl+Shift+R (или Cmd+Shift+R на Mac)
- Или откройте в режиме инкогнито

### 2. Пересобрать frontend
```bash
cd /home/ubuntu
docker-compose stop frontend
docker-compose build frontend
docker-compose up -d frontend
```

### 3. Проверить логи
```bash
docker-compose logs frontend
docker-compose logs backend
```

### 4. Проверить файлы переводов
```bash
cat frontend/public/locales/ru/common.json | grep -E "actions|no_tasks"
cat frontend/public/locales/en/common.json | grep -E "actions|no_tasks"
```

## 📝 Текущий статус

- ✅ Переводы добавлены в файлы
- ✅ Компоненты обновлены для использования переводов
- ✅ Frontend пересобран
- ⏳ Может потребоваться очистка кэша браузера

